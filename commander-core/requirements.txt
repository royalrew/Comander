# Core
python-dotenv==1.0.1
APScheduler==3.10.4
pydantic==2.8.2
bandit==1.7.9

# AI & Routing
litellm==1.41.24
# Replace with actual image/music generation libraries if using official SDKs
# openai==1.35.10 (if needed natively outside litellm)

# Memory & Search
opensearch-py==2.6.0

# Parsing
tree-sitter==0.22.3
# Usually needs language specific bindings like tree-sitter-python, tree-sitter-typescript
tree-sitter-python==0.21.0
tree-sitter-typescript==0.21.2

# API & Web (if exposing internal API)
fastapi==0.111.0
uvicorn==0.30.1
requests==2.32.3

# Storage (Cloudflare R2 is S3 compatible)
boto3==1.34.144

# Communication (Telegram)
aiogram==3.10.0

# Finance (CFO module)
stripe==10.4.0

# Database
psycopg2-binary==2.9.9 # or asyncpg if fully async
sqlalchemy==2.0.31
